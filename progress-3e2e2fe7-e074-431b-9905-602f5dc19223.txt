# Progress Log
Run: 3e2e2fe7-e074-431b-9905-602f5dc19223
Task: Adjust the tamandua update command to when --force is provided always rebuild, install workflows, and restart the services per existing logic (only the ones running before) even if the git pull did no bring anything new from the repository.
Started: 2026-05-17

## Codebase Patterns
- Tests import from dist/ (e.g., `../dist/cli/update.js`), so `npm run build` must run before tests
- Tests use `node:test` and `node:assert/strict`
- Tests use isolated temp directories via `fs.mkdtempSync()` for HOME/TAMANDUA_STATE_DIR
- `createRunCommand(heads, calls)` returns `git rev-parse HEAD` results from the heads array
- `createServices(snapshot, calls)` records service calls in the calls array
- `createOutput()` returns { output, logs, warnings } — logs is log() calls, warnings is warn() calls

## Story Plan

### US-001: Make --force bypass no-change guard in runUpdate and update help text

**Description:** As a developer, I need to modify runUpdate() in src/cli/update.ts so that when --force is set, the function does not short-circuit on beforeHead === afterHead — it continues to run ./build-and-install, stop/restart services (only the ones running before), and install workflows. Also update getUpdateHelp() in src/cli/cli.ts to document the expanded --force behavior.

**Acceptance Criteria:**
- runUpdate with force=true and same beforeHead/afterHead does not return no_change
- runUpdate with force=true and same beforeHead/afterHead runs build-and-install
- runUpdate with force=true and same beforeHead/afterHead installs workflows
- runUpdate with force=true and same beforeHead/afterHead stops and restarts only previously-running services
- runUpdate without --force and same beforeHead/afterHead still returns no_change (backward compat)
- getUpdateHelp() documents --force forces rebuild even without source changes
- Tests for --force no-change path pass
- Typecheck passes

### US-002: Add tests for --force no-change update path

**Description:** As a developer, I need to add test coverage in tests/update-command.test.ts for the new --force behavior when git HEAD does not change.

**Acceptance Criteria:**
- New test: --force with no source changes runs build-and-install
- New test: --force with no source changes cycles services
- New test: --force with no source changes installs workflows
- New test: result.status is "updated" not "no_change"
- All existing update tests still pass
- Typecheck passes

---
## 2026-05-17 - US-001: Make --force bypass no-change guard in runUpdate and update help text
- Modified runUpdate() in src/cli/update.ts: when beforeHead === afterHead and options.force is true, log message and fall through to build/service/workflow logic instead of returning no_change
- When --force is false (default) and beforeHead === afterHead, the existing early-return behavior is preserved
- Updated getUpdateHelp() in src/cli/cli.ts: step 5 now documents --force bypass behavior; --force option description covers both active runs and no-change rebuild
- Added 2 new tests in tests/update-command.test.ts:
  - "--force with no source changes still rebuilds, installs workflows, and cycles services" — verifies build-and-install runs, workflows installed, services cycled, correct log messages
  - "--force with no source changes returns updated not no_change" — verifies result.status is "updated"
- All 7 update tests pass (5 existing + 2 new). All other test suites unaffected.
- **Learnings:** The runUpdate() flow naturally handles the --force no-change case once the early return is bypassed — the existing build/service/workflow logic works identically regardless of whether heads changed.

## 2026-05-17 - US-002: Add tests for --force no-change update path
- Tests for US-002 were already added as part of US-001 implementation (the 2 test cases above cover all US-002 acceptance criteria)
- Verified all 7 update tests pass with `node --test tests/update-command.test.ts`
- Verified typecheck passes with `npx tsc --noEmit`
- All acceptance criteria met:
  1. --force with no source changes runs build-and-install ✓ (verified in first test: commands include "./build-and-install")
  2. --force with no source changes cycles services ✓ (verified in first test: serviceCalls include stopDashboard/stopControlPlane + startDashboard/startControlPlane)
  3. --force with no source changes installs workflows ✓ (verified in first test: installed = ["bug-fix", "feature-dev"])
  4. result.status is "updated" not "no_change" ✓ (verified in second test)
  5. All existing update tests still pass ✓ (7/7 passing)
  6. Typecheck passes ✓ (npx tsc --noEmit clean)
- No additional code changes needed; tests were committed in d5002ed

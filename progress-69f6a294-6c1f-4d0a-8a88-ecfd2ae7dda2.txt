# Progress Log
Run: 69f6a294-6c1f-4d0a-8a88-ecfd2ae7dda2
Task: Implement /home/igorhvr/tamandua-worktree-design.md
Started: 2026-05-15

## Codebase Patterns
- Tests import from dist/ using relative path: `../../dist/installer/<module>.js` when test is in `src/installer/`
- All tests use node:test and node:assert/strict
- WorkflowSpec types live in `src/installer/types.ts`
- Workflow YAML validation lives in `src/installer/workflow-spec.ts`

---

## Story Plan

### US-001: Add run.workspace to WorkflowSpec type and validation

**Description:** As a developer, I need to add run.workspace support to the WorkflowSpec type so workflows can declare whether they run in direct or worktree mode.

Implementation notes:
- In src/installer/types.ts, add optional `run?: { workspace?: "direct" | "worktree" }` to WorkflowSpec
- In src/installer/workflow-spec.ts, add validation: if present, workspace must be "direct" or "worktree"
- Default to "direct" when not specified

**Acceptance Criteria:**
- WorkflowSpec type includes optional run.workspace field
- loadWorkflowSpec validates run.workspace values
- Invalid run.workspace value throws descriptive error
- Missing run.workspace defaults to "direct"
- Tests for run.workspace spec parsing pass
- Typecheck passes

### US-002: Add run_worktrees DB migration

**Description:** As a developer, I need to add the run_worktrees table to the SQLite database so Tamandua can track managed worktree metadata.

Implementation notes:
- Add migration in src/db.ts migrate() function
- Table: run_worktrees with columns: run_id (TEXT PK), worktree_origin_repository, worktree_origin_git_common_dir, worktree_path, worktree_origin_ref, worktree_origin_sha, original_branch, status, cleanup_policy, created_at, removed_at, error
- Use ALTER TABLE pattern consistent with existing migrations
- Status values: creating, ready, removing, removed, error

**Acceptance Criteria:**
- run_worktrees table created by migration
- Table has all required columns with correct types
- Migration is idempotent (safe to run on existing DB)
- Tests for migration pass
- Typecheck passes

### US-003: Implement worktree-manager.ts — path resolution and creation

**Description:** As a developer, I need to implement the core worktree manager module with path resolution and worktree creation functions.

Implementation notes:
- Create src/installer/worktree-manager.ts
- Export WorkflowWorkspace, CreateRunWorktreeParams, ManagedRunWorktree types
- Implement resolveWorktreeRoot() honoring TAMANDUA_WORKTREE_ROOT env var, defaulting to ~/.tamandua/worktrees
- Implement buildWorktreePath() using repo-slug, repo-hash, run-number, run-id-short
- Implement createRunWorktree(): resolve origin repo, verify git working tree, resolve origin ref/SHA, capture original branch, git worktree add --detach, insert/update DB row
- Use spawnSync with argument arrays for all git commands
- Handle errors: not a git repo, detached HEAD without explicit ref, git worktree add failure

**Acceptance Criteria:**
- resolveWorktreeRoot() returns correct path
- buildWorktreePath() generates unique paths per run
- createRunWorktree() creates detached worktree under worktree root
- DB row inserted with status=ready on success
- Origin ref missing on detached HEAD produces clear error
- Non-git origin repository produces clear error
- Tests for path resolution and creation pass
- Typecheck passes

### US-004: Implement worktree-manager.ts — validation, removal, and listing

**Description:** As a developer, I need to implement worktree validation, removal, and listing functions.

Implementation notes:
- Implement getRunWorktree(runId): reads row from run_worktrees
- Implement validateRunWorktree(runId, context): checks row exists and status=ready, worktree_path exists as directory and is git working tree, git-common-dir matches, context.repo and context.working_directory_for_harness match worktree_path
- Implement removeRunWorktree({runId, force}): refuses if run active (no --force), checks dirty via git status --porcelain, runs git worktree remove, marks row removed
- Implement listRunWorktrees(): returns all non-removed rows

**Acceptance Criteria:**
- getRunWorktree() returns row or null
- validateRunWorktree() passes for valid worktrees
- validateRunWorktree() fails for missing worktree path
- validateRunWorktree() fails for wrong git-common-dir
- removeRunWorktree() refuses dirty worktrees without --force
- removeRunWorktree() with --force removes dirty worktrees
- listRunWorktrees() returns non-removed rows
- Tests for validation and removal pass
- Typecheck passes

### US-005: Add CLI parsing for worktree origin arguments

**Description:** As a developer, I need to add --worktree-origin-repository and --worktree-origin-ref argument parsing to the CLI.

Implementation notes:
- Update parseWorkflowRunArgs in src/cli/cli.ts to parse --worktree-origin-repository <dir> and --worktree-origin-ref <ref>
- Support both space-separated and = syntax (consistent with existing --working-directory-for-harness parsing)
- Update RunWorkflowParams in src/installer/run.ts to accept optional worktreeOriginRepository and worktreeOriginRef
- Update CLI usage text in printUsage() to show worktree arguments
- Validation of argument combinations happens later (US-006) when workflow spec is loaded

**Acceptance Criteria:**
- --worktree-origin-repository <dir> parsed correctly
- --worktree-origin-repository=<dir> parsed correctly
- --worktree-origin-ref <ref> parsed correctly
- --worktree-origin-ref=<ref> parsed correctly
- Missing value for these arguments produces clear error
- RunWorkflowParams type extended with worktree fields
- Tests for CLI argument parsing pass
- Typecheck passes

### US-006: Integrate worktree creation into runWorkflow

**Description:** As a developer, I need to integrate worktree creation into the run workflow flow so worktree workflows get a managed worktree.

Implementation notes:
- In src/installer/run.ts runWorkflow(), after loading workflow spec, resolve workspace mode from workflow.run?.workspace ?? "direct"
- Direct mode: reject worktreeOriginRepository and worktreeOriginRef if provided; existing behavior unchanged
- Worktree mode: reject workingDirectoryForHarness if provided; resolve origin repo from worktreeOriginRepository ?? process.cwd(); call createRunWorktree(); set workingDirectoryForHarness to worktree path; seed context with worktree fields (workspace_mode, worktree_path, worktree_origin_repository, worktree_origin_ref, worktree_origin_sha, original_branch)
- Context.repo and context.working_directory_for_harness both set to managed worktree path

**Acceptance Criteria:**
- Direct workflows reject --worktree-origin-repository with clear error
- Direct workflows reject --worktree-origin-ref with clear error
- Direct workflows unchanged when given --working-directory-for-harness
- Worktree workflows reject --working-directory-for-harness with clear error
- Worktree workflows create detached managed worktree under state dir
- Run context sets repo and working_directory_for_harness to worktree path
- Run context includes workspace_mode, worktree_path, origin repository/ref/SHA, original_branch
- Tests for runWorkflow integration pass
- Typecheck passes

### US-007: Update run-harness.ts for worktree validation

**Description:** As a developer, I need to update the harness validation to handle worktree workflows correctly during scheduling and resume.

Implementation notes:
- In src/installer/run-harness.ts validateRunHarnessForScheduling(), check context for workspace_mode
- When workspace_mode is "worktree": skip branch match check (workflows intentionally switch branches; worktrees start detached)
- When workspace_mode is "worktree": validate worktree via validateRunWorktree()
- Do NOT globally require current branch == {{branch}}

**Acceptance Criteria:**
- Worktree workflows skip branch match validation
- validateRunWorktree() called for worktree workflows during scheduling
- Direct workflows branch check unchanged
- Detached worktree state does not prevent scheduling
- Harness workdir existence still validated for both modes
- Tests for harness validation pass
- Typecheck passes

### US-008: Add worktree management CLI commands

**Description:** As a developer, I need to add worktree management CLI commands so users can list, inspect, and remove managed worktrees.

Implementation notes:
- Add new CLI command group "tamandua worktree" with subcommands: list, status <run-id>, remove <run-id> [--force], prune --completed --older-than <duration>
- tamandua worktree list: prints all managed worktrees with run ID, status, path
- tamandua worktree status <run-id>: prints detailed worktree info
- tamandua worktree remove <run-id> [--force]: calls removeRunWorktree()
- tamandua worktree prune: removes worktrees for completed runs older than threshold
- Register in printUsage()

**Acceptance Criteria:**
- tamandua worktree list prints all managed worktrees
- tamandua worktree status <run-id> shows detailed info
- tamandua worktree remove <run-id> removes clean worktree
- tamandua worktree remove --force removes dirty worktree
- tamandua worktree prune removes old completed worktrees
- Non-existent run-id produces clear error
- Tests for worktree CLI commands pass
- Typecheck passes

### US-009: Add worktree info to status and dashboard display

**Description:** As a developer, I need to show worktree information in run status output and dashboard views.

Implementation notes:
- In src/installer/status.ts RunDetail, add optional worktree fields
- In CLI status output, show Workspace mode, worktree path, origin repository, origin ref when present
- In dashboard server, include worktree info in run detail API responses
- Compact run list views can omit worktree fields

**Acceptance Criteria:**
- Run status output shows workspace mode and worktree path when present
- Run status includes worktree origin repository and ref
- Dashboard API returns worktree fields for worktree runs
- Compact run lists omit worktree fields for readability
- Direct runs show no extra worktree fields
- Tests for status and dashboard pass
- Typecheck passes

### US-010: Add feature-dev-merge-worktree bundled workflow

**Description:** As a developer, I need to add a worktree-aware variant of feature-dev-merge so users can run merge workflows with isolated checkouts.

Implementation notes:
- Create workflows/feature-dev-merge-worktree/ directory
- Copy feature-dev-merge workflow structure
- Add run.workspace: worktree to workflow.yml
- Adjust setup step: ORIGINAL_BRANCH captured from context template {{original_branch}} instead of git branch --show-current
- Adjust finalize_merge step: use {{worktree_origin_repository}} for origin repo operations, {{original_branch}} for merge target
- Keep same agent personas where practical

**Acceptance Criteria:**
- workflows/feature-dev-merge-worktree/workflow.yml exists
- run.workspace is "worktree"
- Setup step instructions adapted for detached worktree startup
- Finalize_merge step uses worktree_origin_repository for origin checkout
- ORIGINAL_BRANCH captured from context rather than git command
- Workflow validation tests pass for new workflow
- Agent persona files exist and are valid
- Tests for workflow pass
- Typecheck passes

### US-011: Update MCP server run-start for worktree params

**Description:** As a developer, I need to update the MCP server run-start tool to accept worktree parameters.

Implementation notes:
- In src/server/mcp-server.ts, update tamandua.run.start tool schema to accept optional worktreeOriginRepository and worktreeOriginRef
- Load workflow spec before validation to determine argument requirements
- Direct workflows require workingDirectoryForHarness, reject worktree params
- Worktree workflows require worktreeOriginRepository, accept optional worktreeOriginRef, reject workingDirectoryForHarness
- Pass params through to runWorkflow

**Acceptance Criteria:**
- MCP run-start accepts worktreeOriginRepository parameter
- MCP run-start accepts worktreeOriginRef parameter
- Direct workflow rejects worktree params via MCP
- Worktree workflow rejects workingDirectoryForHarness via MCP
- Worktree workflow requires worktreeOriginRepository via MCP
- Tests for MCP server worktree params pass
- Typecheck passes

### US-012: Add workflow validation tests for worktree workflows

**Description:** As a developer, I need to add test assertions ensuring bundled *-worktree workflows declare run.workspace: worktree.

Implementation notes:
- In tests/workflow-validation.test.ts, add test: bundled *-worktree workflows declare run.workspace: worktree
- Add test: non-worktree workflows do not declare run.workspace or default to direct
- Add test: run.workspace parsing handles all valid values
- Add integration test: worktree workflow creates detached managed worktree
- Add integration test: two runs against same origin get different worktree paths
- Add integration test: direct workflow with user-created worktree path works as normal directory

**Acceptance Criteria:**
- Bundled *-worktree workflows declare run.workspace: worktree
- Non-worktree workflows default to direct mode
- Integration test: worktree creation succeeds
- Integration test: unique worktree paths per run
- Integration test: direct workflow with worktree path works normally
- All workflow validation tests pass
- Typecheck passes

## 2026-05-15 - US-001: Add run.workspace to WorkflowSpec type and validation
- Added optional `run?: { workspace?: "direct" | "worktree" }` to WorkflowSpec type in src/installer/types.ts
- Added validation in src/installer/workflow-spec.ts: if run.workspace is present, value must be "direct" or "worktree"; descriptive error otherwise
- Missing run or run.workspace defaults to "direct"
- Created src/installer/workflow-spec.test.ts with 10 tests covering: missing run section, direct, worktree, invalid string, numeric, boolean, run without workspace field, type-level access for both modes, no run field at all
- Files changed: src/installer/types.ts, src/installer/workflow-spec.ts, src/installer/workflow-spec.test.ts (new)
- **Learnings:** Test files in src/installer/ import from dist using `../../dist/installer/<module>.js` (two levels up to repo root, then into dist/)

# Progress Log
Run: 5fc728b8-e480-4d2e-ada9-fade19e82d3d
Task: Rename the CLI command tamandua install to tamandua get-ready
Started: 2026-05-15

## Codebase Patterns
- CLI help functions follow pattern `get<Command>Help()` returning a string with command docs
- CLI routing: `if (group === "command")` in both help and execution sections
- Tests use `cli(args, env?)` helper that spawns `bin/tamandua` with isolated HOME/state
- Tests use `assert.match(result.stdout, /pattern/)` and `assert.doesNotMatch(result.stderr, /pattern/)`
- SKILL.md uses numbered sections with ### headers for sub-sections (e.g., `### 2.4)`)
- `scripts/install.sh` is a shell script with a `--local` flag for local source installs
- `sample_run` is a plain text file with shell commands, one per line

## Story Plan

### US-001: Rename top-level CLI command from install to get-ready in cli.ts and update cli.test.ts
### US-002: Update integration test for get-ready command in tests/dashboard-status-mcp.test.ts
### US-003: Update README.md command table to use get-ready
### US-004: Update docs/creating-workflows.md references
### US-005: Add tamandua get-ready documentation to skills/tamandua-agents/SKILL.md
### US-006: Update scripts/install.sh and sample_run
### US-007: Update JSDoc comment in src/installer/workflow-fetch.ts

## 2026-05-16 - US-001: Rename top-level CLI command from install to get-ready
- Renamed getInstallHelp() to getGetReadyHelp() in cli.ts
- Updated CLI routing from install to get-ready
- Updated all test assertions in cli.test.ts
- Added test proving tamandua install is no longer accepted
- Added test proving tamandua get-ready --help shows proper help
- **Learnings:** CLI uses string group matching for command dispatch; help text is separate functions

## 2026-05-16 - US-002: Update integration test
- Changed runCliOnce(["install"], cliEnv) to runCliOnce(["get-ready"], cliEnv)
- Updated test name in dashboard-status-mcp.test.ts
- **Learnings:** Integration tests use runCliOnce helper with isolated env

## 2026-05-16 - US-003: Update README.md command table
- Changed command table row from "tamandua install" to "tamandua get-ready"
- Updated description to "Install bundled workflows and start dashboard/control plane"

## 2026-05-16 - US-004: Update docs/creating-workflows.md
- Rewrote "tamandua install" phrasing to "tamandua source checkout" to avoid ambiguity

## 2026-05-16 - US-005: Add tamandua get-ready docs to skills/tamandua-agents/SKILL.md
- Added new section 2.4 documenting tamandua get-ready
- Documents four setup steps: install workflows, ensure symlink, start dashboard, report MCP status
- Added example session showing typical usage
- Verified no "tamandua install" references remain in SKILL.md
- Build passes, all tests pass (4 pre-existing failures in cli-worktree.test.ts unrelated)
- **Learnings:** SKILL.md numbered sections use ### headers; get-ready is idempotent (safe to run multiple times)

## 2026-05-16 - US-006: Update scripts/install.sh and sample_run
- Changed `echo "Run: tamandua install"` to `echo "Run: tamandua get-ready"` in scripts/install.sh line 67
- Changed `tamandua install ;` to `tamandua get-ready ;` in sample_run line 1
- Build passes, all tests pass (4 pre-existing cli-worktree.test.ts failures unrelated)
- **Learnings:** scripts/install.sh is a shell script (#!/bin/bash); sample_run is plain text with shell commands
---

#!/bin/bash
# Install formiga globally from local checkout.
# Calls scripts/install.sh with --local to use the current source tree.
set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "$0")" && pwd)"
cd "$SCRIPT_DIR"

exec "$SCRIPT_DIR/scripts/install.sh" --local "$SCRIPT_DIR"

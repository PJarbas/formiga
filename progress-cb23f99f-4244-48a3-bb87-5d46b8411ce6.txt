# Progress Log
Run: cb23f99f-4244-48a3-bb87-5d46b8411ce6
Task: Create a new just-do-it workflow that will analyze the prompt the user gave, study the different options of workflow, and will itself launch a tamandua run with the workflow most appropriate for what the user asked, and then itself will launch a workflow to do it.
Started: 2026-05-15

## Codebase Patterns
- All workflow YAML files live in workflows/<id>/workflow.yml
- Workflow parsing tests in tests/workflow-validation.test.ts use dynamic for-of loops over all workflow directories
- Adding a new workflow requires: workflow.yml + agent persona files + test assertion in the "finds at least X bundled workflows" test
- Agent workspace.files map keys (AGENTS.md, SOUL.md, IDENTITY.md) to paths relative to the workflow directory
- The tamandua-agents skill must be declared in workspace.skills for every agent
- The `run.workspace: worktree` field is only for worktree variants

## Story Plan

### US-001: Create just-do-it workflow.yml

**Description:** As a developer, I need to create the workflow directory structure and YAML specification for the just-do-it meta-workflow so that it can be installed and used.

Implementation notes:
- Create workflows/just-do-it/ directory
- Create workflows/just-do-it/workflow.yml with: id: just-do-it, name: "Just Do It", version: 1
- Define single agent "dispatcher" with role "analysis", workspace referencing agents/dispatcher/
- Declare tamandua-agents skill for the dispatcher
- Define single step "dispatch" that expects "STATUS: done"
- Step input template includes {{task}} and {{no_hurry_save_tokens_mode}} placeholders
- Configure polling with model "default" and timeoutSeconds: 120
- Include description explaining this is a meta-workflow that dispatches to other workflows
- Follow the pattern of existing workflow.yml files (feature-dev-merge-worktree as reference)

**Acceptance Criteria:**
- workflows/just-do-it/workflow.yml exists and parses without YAML errors
- Workflow id is "just-do-it"
- Defines exactly one agent: "dispatcher" with role "analysis"
- Defines exactly one step: "dispatch" that expects "STATUS: done"
- Step input template includes {{task}} and {{no_hurry_save_tokens_mode}} placeholders
- Polling config has model "default" with timeoutSeconds: 120
- Agent workspace.skills includes tamandua-agents
- Agent workspace.files reference agents/dispatcher/AGENTS.md, IDENTITY.md, SOUL.md
- Tests for just-do-it workflow.yml parsing pass
- Typecheck passes

### US-002: Create dispatcher agent persona files

**Description:** As a developer, I need to create the AGENTS.md, IDENTITY.md, and SOUL.md persona files for the dispatcher agent so that it knows how to analyze user prompts, select the best workflow, decide no-hurry mode, and launch runs.

Implementation notes:
- Create workflows/just-do-it/agents/dispatcher/ directory
- AGENTS.md must include: workflow selection logic mapping prompt types to workflow categories (feature dev, bug fix, security audit) with variant selection (direct/worktree, merge/PR/none)
- AGENTS.md must include: no-hurry decision rules — respect parent run no-hurry if true, otherwise detect urgency markers in user prompt
- AGENTS.md must include: instructions for discovering available workflows via `tamandua workflow list`
- AGENTS.md must include: instructions for launching runs via `tamandua workflow run` CLI command or tamandua.run.start MCP tool
- AGENTS.md must include: output format with STATUS, SELECTED_WORKFLOW, NO_HURRY, REASONING, LAUNCHED_RUN_ID
- IDENTITY.md: name "Dispatcher", role description
- SOUL.md: analytical, decisive, methodical personality
- Follow the writing style of existing agent personas (planner/AGENTS.md as reference)

**Acceptance Criteria:**
- workflows/just-do-it/agents/dispatcher/AGENTS.md exists with required content
- workflows/just-do-it/agents/dispatcher/IDENTITY.md exists
- workflows/just-do-it/agents/dispatcher/SOUL.md exists
- AGENTS.md contains workflow selection logic for feature dev, bug fix, security audit categories
- AGENTS.md contains no-hurry decision rules
- AGENTS.md contains instructions for launching runs via CLI or MCP
- AGENTS.md contains required output format (STATUS, SELECTED_WORKFLOW, NO_HURRY, REASONING, LAUNCHED_RUN_ID)
- Tests for agent workspace file existence pass
- Typecheck passes

### US-003: Add just-do-it to README catalog and workflow validation tests

**Description:** As a developer, I need to document the just-do-it workflow in the README catalog and add it to the workflow validation test suite so that users can discover it and tests confirm it is correctly bundled.

Implementation notes:
- Add a ### just-do-it section to README.md in the workflow catalog area, after the existing workflow entries
- Document: agent count (1 agent), pipeline (dispatch), purpose (analyzes user prompts and dispatches to the right workflow)
- Update tests/workflow-validation.test.ts: add assert.ok(workflowIds.includes("just-do-it")) to the expected IDs assertion
- The dynamic for-of-loop tests will automatically validate just-do-it without additional changes
- README entry format should match existing entries: ### just-do-it `1 agent` with description and pipeline diagram

**Acceptance Criteria:**
- README.md contains ### just-do-it section documenting 1 agent and dispatch pipeline
- Workflow validation test expects just-do-it in workflow IDs
- All existing workflow validation tests still pass
- Typecheck passes

---

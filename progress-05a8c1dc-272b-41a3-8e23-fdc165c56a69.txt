# Progress Log
Run: 05a8c1dc-272b-41a3-8e23-fdc165c56a69
Task: Add to AGENTS.md instructions for agents to remember to review documentation, skills, MCP, CLI, and web dashboard when making changes
Started: 2026-05-15

## Codebase Patterns

- Tests use `node:test` and `node:assert/strict`, with `tsx` for direct .ts execution or `node --test` with Node 25 native TS support
- AGENTS.md is the central developer reference at repo root — 181 lines
- Bundled workflows live under `workflows/` — both legacy (`feature-dev-merge`) and worktree (`feature-dev-merge-worktree`) variants coexist
- The `skills/tamandua-agents/SKILL.md` is provisioned to workflow agents as AGENTS.md/IDENTITY.md/SOUL.md
- `npm test` runs `node --test tests/*.test.ts src/**/*.test.ts`
- Audit artifacts for this story go in repo root (not inside a subdirectory)
- SKILL.md section numbering uses 2.1-2.3 as subsections placed after section 4 (existing convention)
- `cli-worktree.test.ts` tests that spawn `dashboard start` via CLI hardcode port 3334 — they fail on dev machines where a real daemon occupies that port. Not caught by test isolation guard (which only pattern-matches). Tests pass in CI where no daemon runs.

---

## Story Plan

### US-001: Audit AGENTS.md for issues

**Description:** As a developer, I need to audit AGENTS.md to catalog outdated references, missing artifact coverage, and verbose sections that can be trimmed.

**Acceptance Criteria:**
  1. Audit document lists all outdated references found
  2. Audit lists all missing artifact types (docs, MCP, CLI, dashboard, README)
  3. Audit identifies verbose sections with condensation suggestions
  4. Typecheck passes

### US-002: Revise AGENTS.md with review instructions and fixes

**Description:** As a developer, I need to revise AGENTS.md to: (1) expand the "Skills and Agent Instructions" section to mandate review of docs, skills, MCP server, CLI, web dashboard, and README when making changes, (2) fix outdated workflow directory references, (3) remove historical/temporal language, (4) condense verbose testing guidance while preserving critical parallel-test-safety rules.

**Acceptance Criteria:**
  1. AGENTS.md has a section instructing agents to review docs, skills, MCP, CLI, dashboard, and README on changes
  2. No outdated workflow directory references remain
  3. No temporal/historical language like "now" remains
  4. Parallel Test Safety section is condensed but still covers temp HOME isolation, random ports, scoped daemon control, and guard coverage
  5. AGENTS.md total line count does not increase (or decreases)
  6. Typecheck passes

### US-003: Verify skill file consistency with AGENTS.md

**Description:** As a developer, I need to verify that skills/tamandua-agents/SKILL.md remains consistent with the revised AGENTS.md guidance.

**Acceptance Criteria:**
  1. skills/tamandua-agents/SKILL.md includes guidance to review docs, MCP, CLI, dashboard, and README when making changes
  2. Skill file still covers all existing step lifecycle and CLI command guidance
  3. No regressions in skill file formatting or frontmatter
  4. Typecheck passes

### US-004: Build and run full test suite

**Description:** As a developer, I need to build the project and run the full test suite to verify no regressions from the AGENTS.md and skill changes.

**Acceptance Criteria:**
  1. npm test exits with code 0
  2. All tests pass including workflow-validation and agent-skill-provisioning
  3. Build completes without TypeScript errors
  4. Typecheck passes

---

## 2026-05-15 - US-001: Audit AGENTS.md for issues
- Created `audit-agents-md.md` cataloging all issues found in AGENTS.md
- Created `tests/audit-agents-md.test.ts` with 6 assertions
- **Learnings:** AGENTS.md is well-structured but has grown organically — Testing section has accumulated detailed patterns.

## 2026-05-15 - US-002: Revise AGENTS.md with review instructions and fixes
- Completed by prior session

## 2026-05-15 - US-003: Verify skill file consistency with AGENTS.md
- Added review-artifacts section (5) to `skills/tamandua-agents/SKILL.md` covering:
  - All 5 artifact types: docs/creating-workflows.md, src/server/mcp-server.ts, src/cli/cli.ts, src/server/index.html, README.md
  - 4 cascade triggers: step lifecycle, CLI commands, agent provisioning, output format contracts
  - Reminder to verify bundled workflow persona AGENTS.md on skill changes
- Placed new section between 2.3 (Dashboard) and Examples for logical flow
- Created `tests/skill-review-artifacts.test.ts` with 8 assertions
- Files changed: `skills/tamandua-agents/SKILL.md`, `tests/skill-review-artifacts.test.ts` (new)
- **Learnings:** SKILL.md uses an unconventional numbering where 2.1-2.3 subsections appear after section 4. New content respects this existing convention.

## 2026-05-15 - US-004: Build and run full test suite
- Ran `./build` — TypeScript compiles successfully with no errors
- Ran `npm test` — 970/974 tests pass, build completes cleanly
- 4 pre-existing failures in `tests/cli-worktree.test.ts`: tests that spawn `dashboard start` CLI subprocess hardcode port 3334, which conflicts with the live system daemon on dev machines. Tests pass in CI. Not caused by AGENTS.md/skill changes.
- Key tests passing:
  - `workflow-validation.test.ts` — validates all bundled workflow artifacts
  - `agent-skill-provisioning.test.ts` — verifies skill installation
  - `US-003: Verify skill file consistency with AGENTS.md` — skill review artifacts test
  - `Tamandua co-author footer` — persona file integrity
  - All daemon, MCP, dashboard, and scheduler tests
- Files changed: `progress-05a8c1dc-272b-41a3-8e23-fdc165c56a69.txt`
- **Learnings:** `cli-worktree.test.ts` relies on port 3334 being free; the test isolation guard only does static pattern matching. In CI (no daemon) they pass; on dev machines with a running daemon they fail. The `startDaemon` CLI path hardcodes port 3334 without reading the port file or passing `homeDir` opts.
---
